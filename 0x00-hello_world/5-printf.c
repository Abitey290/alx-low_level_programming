#include <stdio.h>

/**
 * main - printf this sentence "with proper grammar, but the outcome is a piece of art" and returns 0 if the compiled without errors
 * */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art\n");
	return (0);
}
